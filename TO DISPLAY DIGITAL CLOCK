import time

while True:
    current_time = time.strftime("%H:%M:%S")  # 24-hour format
    print("Current Time:", current_time, end="\r")  # \r rewrites in same line
    time.sleep(1)  # updates every 1 second
